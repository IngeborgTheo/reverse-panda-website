/**
 * Public feedback endpoint config for the Contact page.
 * Set `endpoint` to your secure web feedback API (Cloud Function / Worker).
 * Never put Resend API keys or Firebase secrets here.
 */
window.RP_FEEDBACK = {
  endpoint: "",
  supportEmail: "support@reverse-panda.ch",
  source: "website"
};
